# Core Flask and web server
flask==2.3.2                # Updated (LTS version, includes security fixes)
werkzeug==2.3.7             # Explicitly added (Flask dependency)

# Data processing and ML
numpy==1.24.3               # Updated (maintains compatibility with newer versions)
pandas==2.0.3               # Updated (major performance improvements)
scikit-learn==1.3.0         # Updated (new features + bug fixes)
xgboost==1.7.5              # Updated (supports Python 3.8+)
opencv-python==4.8.0.76     # Updated (bug fixes + performance)

# Image processing
pillow==10.0.0              # Updated (security fixes)

# HTTP requests
requests==2.31.0            # Updated (critical security fixes)